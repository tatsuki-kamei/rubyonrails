<div class="container">
	<div class="row">
		<div class="col-xs-3">
			<%= render 'root/side',user: @user,book: @book %>
		</div>
		<div class="col-xs-9">
			<h1>Books</h1>

			<table class="table table-striped">
				<thead>
					<tr>
						<th><p>image</p></th>
						<th><p>title</p></th>
						<th><p>opinion</p></th>
					</tr>
				</thead>
				<tbody>
					<% @user.books.each do |book| %>
						<tr>
							<td><%= link_to  attachment_image_tag @user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "100x100" %></td>
							<td><%= link_to book.title, "/books/#{book.id}"%></td>
							<td><%= book.body %></td>
						</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>